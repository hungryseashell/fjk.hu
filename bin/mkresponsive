#!/bin/bash

source=$1
name=$2
path=$3

convert "$source" -resize 2500 "$path/$name-xl.jpg"
convert "$source" -resize 1440 "$path/$name-l.jpg"
convert "$source" -resize 1024 "$path/$name-m.jpg"
convert "$source" -resize  640 "$path/$name-s.jpg"
